{
  "buildCommand": "NODE_OPTIONS=--openssl-legacy-provider yarn build:es && yarn build:css && node scripts/extract-metadata.js && npx build-storybook -o public",
  "outputDirectory": "public",
  "functions": {
    "api/mcp.mjs": {
      "maxDuration": 30,
      "includeFiles": "api/component-metadata.json"
    }
  }
}
